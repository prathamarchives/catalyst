# 00 — start here

First action: discover where source material lives. Do not read file contents yet.

Run `tools/discover_sessions.py` or replicate its logic. Show discovered locations grouped by source and recommend a safe scan scope. If the user already authorized the recommended safe scan, proceed. If not, ask one approval question.

Then follow the protocol:

```txt
discover → authorize scan → audit → interview → extract → build Catalyst Brain → write skills/workflows/evals → run task-time evaluation → capture feedback → update brain
```

Do not generate final user-facing work before the Catalyst Brain and standards loop exist.
