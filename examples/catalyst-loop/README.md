# catalyst-loop example

A sanitized fictional example showing the loop:

```txt
source → Catalyst Brain rule → task → standards review → feedback → update
```

No private/person-specific demo is required. The useful artifact is the loop a user's own agent runs during real work.
